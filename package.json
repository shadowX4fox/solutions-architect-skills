{
  "name": "solutions-architect-skills",
  "version": "2.13.0",
  "description": "Claude Code plugin for architecture documentation and compliance generation",
  "type": "module",
  "workspaces": ["tools/docgen"],
  "scripts": {
    "build": "bun run build:agents && tsc",
    "build:agents": "bun scripts/build-agents.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.11",
    "typescript": "^5.3.3"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
